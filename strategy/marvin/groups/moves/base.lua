local Base = Class("Group.Move.Base")

Base.MIN_ROBOTS = -1


function Base.canStart()
	error("stub")
end

function Base.chooseRobots(availableRobots)
	error("stub")
end

function Base:init(robots)
end

function Base:canContinue()
	error("stub")
end

function Base:updateTasks()
	error("stub")
end


return Base